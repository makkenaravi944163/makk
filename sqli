For example:

ssh -v invaliduser@iapp192644.croydon.ms.com

or

ssh -vv invaliduser@iapp192644.croydon.ms.com

If the SSH client is installed, you'll typically see output like:

OpenSSH_for_Windows_9.x
debug1: Connecting to iapp192644.croydon.ms.com...
debug1: Connection established.
debug1: Remote protocol version 2.0, remote software version OpenSSH_7.4
debug1: Authentications that can continue: publickey,password

or

debug1: Authentications that can continue: publickey

This helps you determine:

Supported authentication methods (password, publickey, keyboard-interactive, etc.).
Key exchange algorithms.
Host key algorithms.
SSH banner details.
If you know a valid username

You can also use a legitimate account name (without entering a password if you don't have one):

ssh -v username@iapp192644.croydon.ms.com

This only attempts the authentication handshake and provides the same protocol details.

One question

Do you have the Windows OpenSSH client installed? Check by running:

ssh -V

Paste the output, and I'll guide you through the next SSH checks.
