Sanitize user-controlled input before exporting to CSV and prefix formula-triggering characters (=, +, -, @) with a single quote (').
Implement strict server-side input validation using allowlists, length restrictions, and data type validation.
Enforce maximum input length, attachment size, and recipient limits on both the client and server sides.
Remove hard-coded credentials from the source code and securely retrieve secrets from a centralized secrets management solution.
Encrypt all client-server communications using TLS 1.2 or TLS 1.3 with proper certificate validation.
Display generic error messages to users and log detailed exception information securely without exposing internal implementation details.
Digitally sign all executable files using a trusted code-signing certificate before deployment.
Enable Control Flow Guard (CFG) during compilation to mitigate control-flow hijacking attacks.
Enable compiler stack protection (e.g., /GS) to detect and prevent stack-based buffer overflow attacks.
