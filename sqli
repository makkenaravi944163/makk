Your strings command failed

This happened because Windows cannot find strings.exe.

Instead, run it using the full path, for example:

C:\Users\makenna\SysinternalsSuite\strings.exe "\\ms\dist\ird\proj\sumogui\ln.uat\common\release\SumoGUI.exe"

Then search for:

C:\Users\makenna\SysinternalsSuite\strings.exe "\\ms\dist\ird\proj\sumogui\ln.uat\common\release\SumoGUI.exe" | findstr /i "System.ServiceModel"

Also search for:

findstr /i "soap"
findstr /i "http"
findstr /i "endpoint"
findstr /i "binding"
findstr /i "net.tcp"
findstr /i "svc"
