Next Step

Find the actual owner:

select uid,name
from sysusers
order by name
go

Then:

select user_name(uid), name
from sysobjects
where name like '%StreetAcct%'
go

or

select user_name(uid), name
from sysobjects
where name like '%TAPS%'
go

If that works you'll get something like:

dbo      TAPSCustomerDesiStreetAcctV2
owner1   TAPSCustomerDesiStreetAcctV2

Then try:

select top 1 *
from dbo.TAPSCustomerDesiStreetAcctV2
go
Enumerate All Referenced Tables

Since you have source code access, search for all tables:

select name
from sysobjects
where type='U'
and (
      name like '%Account%'
   or name like '%Party%'
   or name like '%Customer%'
   or name like '%Address%'
)
order by name
go
